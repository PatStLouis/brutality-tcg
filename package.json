{
  "name": "brutality-tcg",
  "private": true,
  "version": "0.1.0",
  "description": "Brutality TCG — Discord-first deathcore card collection (PWA + publicly verifiable ledger)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @brutality/web",
    "dev:bot": "npm run dev --workspace @brutality/bot",
    "build": "npm run build --workspace @brutality/web",
    "seed": "npm run seed --workspace @brutality/core",
    "grant": "npm run grant --workspace @brutality/core",
    "redeem": "npm run redeem --workspace @brutality/core",
    "simulate": "npm run simulate --workspace @brutality/core",
    "verify": "npm run verify --workspace @brutality/core"
  },
  "engines": {
    "node": ">=20"
  }
}
